name: First Workflow
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello Raymond"
      - name: Print goodbye
        run: echo "Done and bye"
      - name: Print multi lines
        run: |
            echo "First line"
            echo "Second line"
            echo "Third line"
    
    
